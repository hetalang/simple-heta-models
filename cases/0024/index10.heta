/*
  Ternary complex reaction rate
*/

#setNS {type: abstract}; // set default namespace as abstract

r10 @Reaction {actors: S1 + S2 =>};
r10 := Vmax * S1 * S2 / (Km1 * Km2 + Km2 * S1 + Km1 * S2 + S1 * S2 / Ktc);
